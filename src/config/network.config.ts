export const requestTimeout = 10000; // 请求超时时间
export const successCode = ['200', '0', 200, 0]; // 操作成功code
export const messageDuration = 3000; // message 弹框消失时间
export const isApplyMock = process.env.NODE_ENV === 'development'; // 是否允许使用mock数据
